<% include ../Partials/Header.ejs %>

<nav aria-label="breadcrumb">
    <ol class="breadcrumb">
        <li class="breadcrumb-item"><a href="<%= getAbsUrl('/student/courses') %>">Courses</a></li>
        <li class="breadcrumb-item active"><%= course.getDisplayName() %></li>
    </ol>
</nav>

<h3><%= course.getDisplayName() %></h3>

<% if(!tutorialQuizzes.length){ %>
<p>There is no quiz currently available to you.</p>
<% } else { %>
    <% tutorialQuizzes.forEach(tutorialQuiz => { %>
        <hr class="my-2"/>

        <a href="<%= getAbsUrl(`/student/courses/${course.getId()}/quizzes/${tutorialQuiz._id}/start`) %>"
           class="btn btn-primary btn-sm float-right">Enter</a>

        <b><%= tutorialQuiz.quiz.name %> <small><%= tutorialQuiz.tutorial.getDisplayName() %></small></b>
    <% }) %>
<% } %>

<% include ../Partials/Footer.ejs %>